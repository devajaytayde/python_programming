#1. Read Test Case file from local folder and print
with open("tc_keyword.csv","r") as file:
  for line in file:
    print(line.strip())

#Below is the output of the 1st program
TC_ID,TC_Name,Y,TestSteps,Keyword,ObjectName,ObjectType,Value
1,Open Google Page,Y,Step1,OpenBrowser,,www.google.com
,,Y,Step2,Enter search keyword,txt_search,Atyeti
,,Y,Step3,Click on Text,link_text,Welcome to Atyeti
,,Y,Step3,Click on Text,link_text,Welcome to Atyeti
,,Y,Step4,Close Browser,,

#2. Read Test Case file from local folder and mark "Executed" column as "Y"

#Declaring global variable 
strExecution = ""
iColExecuted = 2

#Reading Test case file
with open("tc_keyword.csv","r") as file:
  for line in file:
    if line != '\n':            
      # Convert Change list value of "Executed" column to "Y" and store in local String variable again   
      lst = line.strip().split(",")
      lst[iColExecuted] = 'Y'
      if strExecution == '':
        strExecution = strTestCasesSep.join(lst)
      else:
        strExecution = strExecution + '\n' + strTestCasesSep.join(lst)
